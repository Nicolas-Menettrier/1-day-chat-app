import React from 'react';

const NoData: React.FC = () => (
  <div className="flex h-screen w-full items-center justify-center">
    <p className="text-2xl font-semibold text-gray-500">No data available</p>
  </div>
);

export default NoData;
